import React from 'react';
import MovieList from '../../components/MovieList';

export const Home: React.FC = () => {
    return (
        <>
            
            <MovieList />
        </>
    );
};

export default Home;